[build]
command = "yarn install --check-cache && yarn build"

[build.environment]
NODE_VERSION = "20.x"
YARN_ENABLE_IMMUTABLE_INSTALLS = "false"

[pages]
framework = "nextjs"
pages_build_output_dir = ".next"  # Use "out" if using `next export`

[build.processing]
skip_source_maps = true